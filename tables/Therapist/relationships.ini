[Sessions]
`Session`.Therapist_id="$id"

[Clients]
__sql__ = "
select distinct c.id, c.name
from `Session` s
 join Visit v on v.Session_id = s.id
 join Client c on v.Client_id = c.id
where s.Therapist_id = '$id'
order by c.name"