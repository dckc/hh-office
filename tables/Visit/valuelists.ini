[Clients]
__sql__ = "select id, name from Client order by name"

[Meetings]
__sql__ = "select s.id, concat_ws(': ', date_format(s.session_date, '%d-%b-%y'), t.name) as name from
Treatment t join Meeting s
on s.treatment_id = t.id"
