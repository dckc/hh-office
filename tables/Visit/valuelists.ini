[Clients]
__sql__ = "select id, name from Client order by name"

[Sessions]
__sql__ = "
select s.id, concat_ws(': ', date_format(s.session_date, '%d-%b-%y'), g.name)
             as name
from `Group` g
join `Session` s on s.Group_id = g.id
order by s.session_date, g.name"
