[Clients]
__sql__ = "select id, name from client order by name"

[Sessions]
__sql__ = "select s.id, concat_ws(': ', date_format(s.session_date, '%d-%b-%y'), g.name) as name from
group_type g join sessions s
on s.group_type = g.id"
